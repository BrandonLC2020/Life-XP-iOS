#!/bin/bash

# --- Dependency Audit ---
echo "Post-checkout dependency audit..."

# Check if project file changed in last checkout
if git diff --name-only ORIG_HEAD HEAD | grep -q "project.pbxproj"; then
  echo "Warning: project.pbxproj has changed. Please verify your dependencies and rebuild."
fi

exit 0
